# Create a file in /tmp

file { 'school':
  content => 'I love Puppet',
  owner   => 'www-data',
  mode    => '0744',
  path    => '/tmp/school',
  group   => 'www-data',
}# Create a file in /tmp

file { 'school':
  content => 'I love Puppet',
  owner   => 'www-data',
  mode    => '0744',
  path    => '/tmp/school',
  group   => 'www-data',
}# Create a file in /tmp

file { 'school':
  content => 'I love Puppet',
  owner   => 'www-data',
  mode    => '0744',
  path    => '/tmp/school',
  group   => 'www-data',
}
